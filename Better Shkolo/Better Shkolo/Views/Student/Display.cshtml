@using Better_Shkolo.Models.Student;
@model StudentViewModel
<br />
<br />
<br />
<br />
<br />
<br />
<br />
<div class="list-group">
    @foreach (var student in Model.Students)
    {
        <p class="list-group-item list-group-item-action">
            @student.FirstName @student.LastName - @student.Email
            <a asp-controller="Absences" asp-action="Add" asp-route-id="@student.Id" asp-route-subjectId="@Model.SubjectId" class="btn btn-primary">Add absence</a>
            <a asp-controller="Mark" asp-action="Add" asp-route-id="@student.Id" asp-route-subjectId="@Model.SubjectId" class="btn btn-primary">Add mark</a>
            <a asp-controller="Review" asp-action="Add" asp-route-id="@student.Id" asp-route-subjectId="@Model.SubjectId" class="btn btn-primary">Add review</a>
        </p>
    }
</div>
